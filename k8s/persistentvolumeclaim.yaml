apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: todo-api-pvc
  namespace: default
  labels:
    app: todo-api
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: standard  # Adjust based on your cluster's storage classes
